@echo off
title LAUNCH WICK MODS - FREE MINT
rem Double-click to deploy the WICK MODS free-mint attachment collection.
rem Asks for your private key privately (never shown, never saved, never in history).
powershell -NoProfile -ExecutionPolicy Bypass -File "%~dp0_helper-launch-mods.ps1"
